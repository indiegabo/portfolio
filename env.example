// This project no longer uses .env for the regular development workflow.
// Edit site-data.config.js for tracked defaults.
// Create site-data.config.local.js for machine-specific overrides.
// Use `npm run sync-data` only when you want to refresh public/profile.json
// or public/blogs.json from external services.
